package esprit.driver.config;

import org.springframework.context.annotation.Configuration;

/**
 * Eureka Configuration Class
 * 
 * Eureka Client Configuration is managed via application.properties
 */
@Configuration
public class EurekaConfig {
    // Configuration properties are handled in application.properties
}
